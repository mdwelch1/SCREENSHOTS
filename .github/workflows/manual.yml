name: Manual Automation

"on":
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate prompt file exists
        run: |
          set -euo pipefail
          test -f amazon_screenshot_automation_prompt.md
          echo "manual workflow syntax is valid and prompt file exists"
